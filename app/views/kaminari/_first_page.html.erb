<%# Link to the "First" page
  - available local variables
    url:           url to the first page
    current_page:  a page object for the currently displayed page
    total_pages:   total number of pages
    per_page:      number of items to fetch per page
    remote:        data-remote
-%>
<li class="<%= 'disabled' if current_page.first? %> page-item prev">
  <%= link_to (current_page.first? ? '#' : url), class: 'page-link', 'aria-label': 'First', remote: remote do %>
    <span aria-hidden="true" class="fa fa-angle-double-left"></span>
    <span class="sr-only">
      <%= t('views.pagination.first').html_safe %>
    </span>
  <% end %>
</li>

